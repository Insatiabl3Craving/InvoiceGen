<UserControl x:Class="InvoiceGenerator.Views.InvoiceHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">
    <Grid Background="White">
        <StackPanel Margin="20">
            <TextBlock Text="Invoice History" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Name="SearchTB" Width="200" Height="38" Padding="10,6" TextChanged="SearchTB_TextChanged" VerticalContentAlignment="Center"/>
                <Button Name="RefreshBtn" Content="Refresh" Width="80" Padding="10" Margin="10,0" Click="RefreshBtn_Click"/>
            </StackPanel>

            <DataGrid Name="InvoiceHistoryDataGrid" AutoGenerateColumns="False" Height="400" MouseDoubleClick="InvoiceHistoryDataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Invoice #" Binding="{Binding InvoiceNumber}" Width="120"/>
                    <DataGridTextColumn Header="Client" Binding="{Binding Client.DisplayName}" Width="200"/>
                    <DataGridTextColumn Header="Date From" Binding="{Binding DateFrom, StringFormat='yyyy-MM-dd'}" Width="120"/>
                    <DataGridTextColumn Header="Date To" Binding="{Binding DateTo, StringFormat='yyyy-MM-dd'}" Width="120"/>
                    <DataGridTextColumn Header="Generated" Binding="{Binding DateGenerated, StringFormat='yyyy-MM-dd HH:mm'}" Width="150"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <Button Name="OpenDocxBtn" Content="Open DOCX" Width="100" Padding="10" Click="OpenDocxBtn_Click" Margin="0,0,10,0"/>
                <Button Name="OpenPdfBtn" Content="Open PDF" Width="100" Padding="10" Click="OpenPdfBtn_Click" Margin="0,0,10,0"/>
                <Button Name="SendEmailBtn" Content="Send Email" Width="100" Padding="10" Click="SendEmailBtn_Click" Margin="0,0,10,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
